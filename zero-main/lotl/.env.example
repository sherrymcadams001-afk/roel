# LotL Controller configuration
# Safer default: bind to localhost. For LAN access, set HOST=0.0.0.0 and open firewall.
HOST=127.0.0.1
PORT=3000
CHROME_PORT=9222

# Readiness/connect probe timeouts (ms)
READY_TIMEOUT_MS=5000
CONNECT_TIMEOUT_MS=8000
